# KM3Aux
"""
    getpmtfile(det_id, run)

Load the `KM3Aux` package to get access to its methods.
"""
function getpmtfile end

"""
    gettripods(det_id, run)

Load the `KM3Aux` package to get access to its methods.
"""
function gettripods end

"""
    gethydrophones(det_id, run)

Load the `KM3Aux` package to get access to its methods.
"""
function gethydrophones end

"""
    getwaveforms(det_id, run)

Load the `KM3Aux` package to get access to its methods.
"""
function getwaveforms end
